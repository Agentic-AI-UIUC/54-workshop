---
description: What we are building this weekend and how the agent must build it
alwaysApply: true
---

# Product

<!-- Fill this in. Keep it under 20 lines. The agent reads it on every prompt. -->

**One flow (the only thing the demo shows):**
User types `___`, agent does `___`, screen shows `___`.

**Stack:** (e.g. Next.js + TypeScript, or Python + FastAPI)

**Rules for the agent:**
- Build only the one flow above. If asked for anything else, say it is out of scope for the demo.
- Prefer the smallest working version. No auth, no DB, no tests unless asked.
- Every agent action must be visible on screen (log line, status badge, toast). Judges need to see it think.
- Never touch `.env`. Read `.env.example` if you need variable names.
- Ask before adding a dependency.
